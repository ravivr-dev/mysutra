import 'package:flutter/material.dart';

class ArticleReplyWidget extends StatelessWidget {
  const ArticleReplyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
